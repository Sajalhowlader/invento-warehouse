import React from 'react';

const ShowUpdateItem = ({ product }) => {
    const { _id, name, img, price, description, quantity } = product
    return (
        <div>

        </div>
    );
};

export default ShowUpdateItem;